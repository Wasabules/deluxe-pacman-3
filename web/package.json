{
  "name": "deluxe-pacman-2-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Web-native rewrite of Deluxe Pacman 2 (TypeScript + Canvas 2D)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "playwright": "^1.61.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "pixi-filters": "^6.1.5",
    "pixi.js": "^8.19.0"
  }
}
